<%- include('includes/head.ejs') %>

<h1><%= titulo %></h1>

<% if (ordenes.length > 0) { %>

    <% for (let orden of ordenes) { %>

            <div class="container">
                <div class="row">
                    <div class="col s12">
                      <div class="card blue-grey darken-1">
                        <div class="card-content white-text">
                          <span class="card-title"><%= titulo %></span>
                          <ul>
                            <li><strong>Nombre: </strong><%= ordenes.nombre %></li>
                            <li><strong>Apellido: </strong><%= ordenes.apellido %></li>
                            <li><strong>Email: </strong><%= ordenes.direccion %></li>
                            <li><strong>Telefono: </strong><%= ordenes.telefono %></li>
                            <li><strong>Direccion: </strong><%= ordenes.direccion %></li>
                          </ul>
                        </div>
                      </div>
                    </div>
                  </div>
            </div>

    <% } %>
<% } else { %>
    <p>No hay ordenes</p> 
<% } %>

<%- include('includes/foot.ejs') %>